{
  "ext.displayName": "Portal",
  "ext.description": "Minimal public MCP endpoint for your VS Code workspace: commands and file transfer only.",
  "view.container.title": "Portal",
  "view.panel.name": "Portal",
  "view.panel.contextualTitle": "Portal",

  "config.title": "Portal",
  "config.tunnelProvider.desc": "Tunnel provider to use when Portal is started.",
  "config.tunnelProvider.enum.ngrok": "ngrok reserved domain — persistent URL",
  "config.tunnelProvider.enum.quick": "Cloudflare Quick Tunnel — random URL on every restart",
  "config.tunnelProvider.enum.named": "Cloudflare Named Tunnel — persistent hostname",
  "config.tunnelProvider.enum.custom": "Custom tunnel — any client via a command template, or attach to an existing URL",
  "config.customTunnelCommand.desc": "Command template run when the provider is `custom`. Placeholders: `{{port}}` (local MCP port), `{{token}}` (route token), `{{workspace}}` (workspace root). Leave empty with `customTunnelUrl` set to attach to an already-running tunnel.",
  "config.customTunnelShell.desc": "Shell used to run `customTunnelCommand`. `default` = cmd.exe on Windows, sh elsewhere.",
  "config.customTunnelUrl.desc": "Fixed public URL for the custom provider (optional). Skips URL extraction from output; without a command, the URL is adopted as-is (attach mode for externally managed tunnels).",
  "config.customTunnelUrlPattern.desc": "Regex applied to tunnel output to extract the public URL; capture group 1 wins when present. Default: the first http(s) URL that is not localhost.",
  "config.customTunnelReadyPattern.desc": "Optional regex; the custom tunnel is only ready after it also matches the tunnel output.",
  "config.customTunnelTimeoutMs.desc": "Startup timeout for the custom tunnel in milliseconds (minimum 5000). Default 30000.",
  "config.ngrokDomain.desc": "Your reserved ngrok domain (e.g. `example.ngrok-free.dev`). Required when the provider is ngrok-reserved.",
  "config.ngrokPoolingEnabled.desc": "Pass `--pooling-enabled` to ngrok when using a reserved domain. Enable this only when you intentionally run multiple Portal/ngrok sessions on the same domain and want ngrok to load balance them.",
  "config.cloudflareDomain.desc": "Public hostname configured on a remotely-managed Cloudflare Tunnel. Named mode also requires a securely stored Tunnel token and a fixed local port.",
  "config.routeToken.desc": "Secret path token. The MCP endpoint is served at `/mcp/<token>`. Leave empty to auto-generate on start.",
  "config.localPort.desc": "Local port for the MCP server. `0` = auto for Quick/ngrok; Cloudflare Named Tunnel requires the same fixed port configured as its origin service.",
  "config.startOnActivation.desc": "Start Portal automatically when VS Code opens a workspace.",
  "config.showCommandsInTerminal.desc": "Mirror `run_command` I/O into a **Portal Agent** terminal tab so you can watch AI-run commands live.",
  "config.maxTransferBytes.desc": "Maximum size in bytes for HTTP file upload/download/pack on the tunnel (`/files/<token>`). Default 64 MiB.",
  "config.promptTemplates.desc": "Pre-authored prompt templates. `{{url}}` inside the text is replaced with the live public MCP URL when the prompt is copied (deterministic providers are even predicted while stopped). Manage them in the Portal settings page or copy via **Portal: Copy Prompt Template**.",

  "config.agentInstructions.desc": "Agent instructions returned to MCP clients during `initialize`. When empty, Portal's built-in default instructions are used. Takes effect the next time Portal starts.",

  "command.start": "Portal: Start",
  "command.stop": "Portal: Stop",
  "command.settings": "Portal: Settings",
  "command.copyUrl": "Portal: Copy URL",
  "command.copyPrompt": "Portal: Copy Prompt Template",
  "command.checkTunnel": "Portal: Check Tunnel",
  "command.installCloudflared": "Portal: Install cloudflared",
  "command.installNgrok": "Portal: Install/Upgrade ngrok",
  "command.setTunnelProvider": "Portal: Set Tunnel Provider...",
  "command.restart": "Portal: Restart",
  "command.switchProfile": "Portal: Switch Connection Profile...",
  "command.tokenSelect": "Portal: Select MCP Session",
  "command.tokenAdd": "Portal: Add MCP Session",
  "command.tokenRemove": "Portal: Remove MCP Session",
  "command.setNgrokDomain": "Portal: Set ngrok Domain...",
  "command.setRouteToken": "Portal: Set Route Token...",
  "command.showLog": "Portal: Show Log",
  "command.showAgentTerminal": "Portal: Show Agent Terminal",

  "config.ngrokConfigPath.desc": "Path to an ngrok agent configuration file passed to ngrok as `--config`. Point each VS Code window at a different file to run two ngrok accounts (two authtokens) side by side. Leave empty to use ngrok\u2019s default config file.",
  "config.ngrokApiPort.desc": "Which local port Portal asks for the ngrok tunnel list. Concurrent ngrok agents cannot share a port \u2014 the second one silently moves to 4041 \u2014 so point the second window at 4041 to avoid a start-up wait. `0` (default) = scan 4040-4045 and match the tunnel by domain (this ngrok build has no `--web-addr` flag).",
  "config.connectionProfiles.desc": "Named connection profiles (tunnel provider, domain, ngrok config file, route token, \u2026). The list is stored globally; manage it from the Portal settings page.",
  "config.tokens.desc": "Independent MCP sessions (each = id, label, routeToken, optional workspacePath and tunnel overrides). Every session publishes its own public MCP endpoint and can run in parallel; the Portal sidebar picks which one is displayed.",
  "config.activeTokenId.desc": "The session id currently shown by the Portal sidebar. Stored per workspace when a folder is open.",
  "config.activeProfile.desc": "Name of the connection profile this workspace uses. Stored in the workspace (.vscode/settings.json) when a folder is open, so several VS Code windows can run different profiles at the same time. Switch it from the sidebar, the settings page, or **Portal: Switch Connection Profile**."
}
